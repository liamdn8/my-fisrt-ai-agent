# Install Ollama

prerequitesite:
- Ollama require zstd
```bash
sudo apt-get install zstd
```

Then following Ollama download page:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

# An easier way

Conect to 9Router and use the free API token, or using CodeX API